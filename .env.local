# --- Mode flags ---
USE_MOCK_AI=true
NODE_ENV=development

# --- Supabase (EU region) ---
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Exposed to frontend (Vite requires VITE_ prefix)
VITE_SUPABASE_URL=https://iavtnohjtmdlpfgkxgei.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImlhdnRub2hqdG1kbHBmZ2t4Z2VpIiwicm9sZSI6ImFub24iLCJpYXQiOjE3Nzg2OTE5MzUsImV4cCI6MjA5NDI2NzkzNX0.qpdAlTlo9HL4v996i37AzWP-nlGPUhhjROFFnrwx1IQ

# --- OpenAI ---
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o

# --- Google Search Console OAuth ---
GSC_CLIENT_ID=
GSC_CLIENT_SECRET=
GSC_REDIRECT_URI=http://localhost:3000/api/gsc/callback

# 32-byte hex key for AES-256-GCM encryption of stored OAuth tokens.
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
TOKEN_ENCRYPTION_KEY=

# 32-byte hex secret used to HMAC-sign the OAuth `state` parameter.
# Different value than TOKEN_ENCRYPTION_KEY. Rotate freely — only affects
# in-flight authorization redirects (10 min TTL).
OAUTH_STATE_SECRET=

# --- Mail (optional) ---
SMTP_URL=

# --- Limits ---
AUDIT_TTL_DAYS=180
ANON_AUDITS_PER_IP_PER_HOUR=5
FREE_AUDITS_PER_MONTH=3
FREE_TRACKED_KEYWORDS=5
